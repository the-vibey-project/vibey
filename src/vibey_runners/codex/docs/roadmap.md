# Roadmap

**Goal:** the OpenAI member of the runner family — taxonomy-faithful capacity classification over Codex/GPT and a generated OpenAI SDK CLI.

This roadmap is **living** (vibey-gh#211): it stays active until the goal above is
achieved and every active contributor has clearly agreed the project is done —
neither is ever assumed. Changes requested while this file is stale are treated as
a risk requiring human confirmation.

## Now

- Doctrine wave: paper published; docs at the arc.
- Code-first classification (rate_limit_exceeded vs insufficient_quota) in production.

## Next

- Keep the generated SDK surface tracking OpenAI's error taxonomy and endpoints.
- Lexicon updates for codeless failure prose as observed.

## Done means

A quarter of vendor churn absorbed without core changes, and contributors sign off the transplant is complete.

*Maintained by the contributors; the exact-head review flags this page when it goes
stale against the changelog.*
