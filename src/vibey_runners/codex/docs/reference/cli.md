# CLI reference

```bash
codexloop --help
codexloop run --help
codexloop api --help
codexloop doctor
codexloop capacity
codexloop watch [--replay]
```

Generated REST commands mirror the installed `openai` SDK surface.
