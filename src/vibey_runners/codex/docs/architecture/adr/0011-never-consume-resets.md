# ADR 0011 — Never auto-consume banked rate-limit resets

## Status

Accepted

## Context

Recorded from the architecture seed list.

## Decision

`account/rateLimitResetCredit/consume` is never sent.

## Consequences

Documented in the architecture roadmap; CI enforces the load-bearing ones.
