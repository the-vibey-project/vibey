# ADR 0002 — Subprocess `codex exec --json` over an SDK

## Status

Accepted

## Context

Recorded from the architecture seed list.

## Decision

Drive the documented CLI rather than an unstable Python agent SDK.

## Consequences

Documented in the architecture roadmap; CI enforces the load-bearing ones.
