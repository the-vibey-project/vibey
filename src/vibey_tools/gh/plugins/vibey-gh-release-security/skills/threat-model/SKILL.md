---
name: threat-model
description: This skill should be used when changing permissions, tokens, untrusted PR handling, AI tools, shell commands, or remote mutations.
---
# Threat modeling

Identify protected assets, actors, entry points, trust boundaries, abuse cases, and failure
modes. Enforce least privilege, immutable dependencies, exact-head checks, bounded repair,
argument/path validation, secret redaction, and independent validation.
