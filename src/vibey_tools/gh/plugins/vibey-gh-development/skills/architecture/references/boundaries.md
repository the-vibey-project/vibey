# Boundaries

Configuration parses repository policy. Pure modules classify state. Adapters call GitHub
or Git. CLI translates arguments and errors. Templates define hosted execution. Installer
renders deterministic artifacts. No decision should depend on parsing human log output.
