# PR repair

Use for failed exact-head scans. Separate code failures from operational blocks, inspect
evidence, make the smallest correct edit, never weaken checks, and publish at most one
guarded non-force commit per attempt.
