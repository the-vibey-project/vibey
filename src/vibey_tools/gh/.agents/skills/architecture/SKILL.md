# Architecture

Use for configuration, policy, adapters, CLI, or workflow changes. Follow `AGENTS.md`,
keep decisions deterministic, isolate remote mutation, preserve the dependency-free
runtime, and update template, renderer, dogfood copy, tests, and docs together.
