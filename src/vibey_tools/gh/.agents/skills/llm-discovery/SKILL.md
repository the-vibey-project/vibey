# LLM discovery

Use for robots, sitemap indexes, canonical URLs, social metadata, JSON-LD, `llms.txt`, and
`llms-full.txt`. Production is canonical/indexable by default; Preview is noindex by
default. Generate repository-specific absolute URLs and immutable provenance.
