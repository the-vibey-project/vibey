# Conflict resolution

Use after materializing the exact unresolved path set. Preserve both intents, edit only
conflicted paths, run normal CI afterward, and never delete or force-push permanent branches.
