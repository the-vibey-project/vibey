#!/bin/sh
# Chained from vibey-gh's commit-msg hook. Runs the pre-commit framework's commit-msg
# stage. framework-hook.sh, beside this file, finds that hook in a plain clone and in a
# linked worktree alike, and warns when it is not installed.
exec "$(dirname "$0")/framework-hook.sh" commit-msg "$@"
