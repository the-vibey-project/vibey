# Build output, dependencies and reports: all regenerated by `npm ci`, `npm run compile`,
# `npm run coverage` and `npm run package`.
node_modules/
out/
coverage/
.vscode-test/
*.vsix
# Scratch space on durable storage for tests of code that refuses volatile storage (10.h).
.test-scratch/
