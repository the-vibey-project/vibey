<!-- audit: TOO-BIG — 3 production modules, 11 behaviours and 16 `gh` sites (pr_automation 761 lines, github_state, install) plus ~20 patched tests; also STALE: keeps `monkeypatch.setattr(pa, "fetch_pr"/"upsert_state", …)` module patches (9.b/D7), misses `pa._gh_json` patch at test_pr_automation.py:603 and 7 CLI install tests that would reach forgejo.local, "Part 1 block" not inlined -->
<!-- title: feat(gh): the durable state comment reaches the forge only through the adapter -->
Part of the **QwenStorm for 3.0.0**. A local sovereign lane (qwenloop until ADR-0046's rename lands) implements this issue; the result is reviewed and verified before it becomes a pull request.

## Why
Sub-doctrine 8.b (`src/vibey_tools/gh/docs/doctrines.md:138-139`) makes self-hosted Forgejo
the default forge, with GitHub and GitLab declared-only; `[platform] kind` defaults to
`forgejo` (`vibey_gh/config.py:288`). The durable automation-state comment that PR
automation, issue automation and conversations share is written by
`github_state.upsert_comment` (`vibey_gh/github_state.py:79-134`), which resolves the
repository with `github_state.repository()` (70-75) and runs `gh pr|issue comment`,
`gh api repos/<R>/issues/comments/<id> --method PATCH` or a GraphQL mutation through the
module's `GhTransport`. It ignores `[platform]`.

It is the one write the other automation modules share (`pr_automation.py:553-563`,
`issue_automation.py:360-370`, `conversation.py:399-411`), so it moves first, on its own.
The rest of the original issue (PR automation's reads, labels and fork mirror; install
notices) is split into child lanes 2-4 below.

When a paid forge is declared, 8.b says it "relays through the sovereign host rather than
replacing it" (`doctrines.md:179-184`). That relay belongs behind `ForgeAdapterInterface`;
this lane keeps the write on the interface.

## Depends on
- **Operator prerequisite (not lane work):** this repository's root `.vibey-gh.toml`
  declares `[platform]` / `kind = "github"` on `develop`. Today it has no `[platform]` table
  on the integration branch (`cce648ef`) or `origin/develop` (`e9e6c79f`); the declaration
  exists only as commit `b60dad4c` on the local branch `docs/changelog-2.1.0`. The live PR
  and issue automation workflows write this comment, and without the declaration they would
  write it to `forgejo.local`. 8.b requires a human to write it (`doctrines.md:179-181`).
- #332 (forge-0a): `ForgeSelector.resolve`, `repository_name`, `for_repository`,
  `RecordingForge` (`test/forge_doubles.py`).
- #335 (forge-0d): `comment_on_change_request(number, body)`.
- #336 (forge-0e): `comment_on_issue(number, body)` and `update_comment(comment, body)`
  (on GitHub: the REST edit when the comment carries `databaseId`, the GraphQL mutation
  when it carries only `id`, and the problem `"comment has no ID"` when it carries neither
  — the argv of `test/test_gh_transport.py:448-470`).

## Required behaviour
1. `github_state.upsert_comment(number, body, comments, pattern, *, subject="pr",
   error="could not persist automation state", forge=None)`:
   - `forge = ForgeSelector().resolve(forge)`;
   - bind: `name, problem = forge.repository_name()`; `if problem: raise
     RuntimeError(problem)` (on GitHub this is today's `"gh repo view --json nameWithOwner:
     <stderr>"`); `bound = forge.for_repository(name)`;
   - the existing-comment search (89-94) is unchanged;
   - none found → `ok, problem = bound.comment_on_change_request(number, body)` when
     `subject == "pr"`, else `bound.comment_on_issue(number, body)`;
   - found → `ok, problem = bound.update_comment(existing, body)`;
   - `if not ok: raise RuntimeError(f"{error}: {problem}")` — which reproduces both of
     today's texts, `"<error>: <stderr>"` and `"<error>: comment has no ID"`.
2. `gh_json`, `repository`, `_transport`, `marker_pattern`, `parse_payload` and
   `render_body` are unchanged: `gh_json` and `repository` remain the GitHub CLI facade for
   the GitHub-only commands (`forge-snapshot`, `forecast`; `cli.py:598`, `cli.py:835`) and
   for modules not yet migrated. Rewrite the module docstring's first line ("…exactly one
   GitHub comment" → "…exactly one forge comment") and its last paragraph (14-16): the state
   comment is written through the forge adapter; `gh_json` and `repository` are the GitHub
   CLI facade kept for the commands that are still GitHub-only.
3. Pass-through only, so the callers' own tests can inject the forge:
   `pr_automation.upsert_state(number, state, summary, comments, *, forge=None)`
   (`pr_automation.py:553-563`) and `issue_automation.upsert_state(number, state, summary,
   comments, *, forge=None)` (`issue_automation.py:360-370`) pass `forge=forge` to
   `github_state.upsert_comment`. Nothing else in those two modules changes; their `record`
   functions still call `upsert_state` without `forge` (child lanes 2 and #343 thread it).
   `conversation.py` is not touched (#343).
4. On GitHub the argv and working directory are unchanged byte for byte when the caller
   passes `GitHubForge(root=<the process's working directory>)`; with no forge injected,
   `resolve` builds the adapter for `load_config().root`, so `gh` runs there (D1, same
   repository). D5: a missing `gh` raises `RuntimeError(problem)`.
5. The function stays module-level: its facade comment (78) already gives the reason.
6. Check there is no import cycle: `python -c "import vibey_gh.github_state,
   vibey_gh.pr_automation, vibey_gh.issue_automation, vibey_gh.forge_selector"` succeeds.

## Where to change
`src/vibey_tools/gh/vibey_gh/github_state.py` (`upsert_comment`, docstring);
`vibey_gh/pr_automation.py` (`upsert_state` only); `vibey_gh/issue_automation.py`
(`upsert_state` only). Imports in `github_state.py`:
`from vibey_gh.forge_selector import ForgeSelector`,
`from vibey_gh.interfaces.forge_adapter_interface import ForgeAdapterInterface`.
Tests: `test/test_gh_transport.py`, `test/test_pr_automation.py` (one test),
`test/test_issue_automation.py` (one test), `test/test_github_state_forge.py` (new).

## Acceptance criteria
- [ ] `github_state.py`'s only `gh` argv is in `gh_json`/`repository`; `upsert_comment`
  calls no transport directly (`grep -n '_transport.run' vibey_gh/github_state.py` finds
  nothing).
- [ ] `test/test_gh_transport.py`'s `upsert_comment` witness tests (471-531) pass with only
  `forge=GitHubForge(root=workdir)` added to the "after" lambdas — byte-for-byte, same cwd.
- [ ] No test reaches `upsert_comment` without `forge=` or a patched caller: in particular
  `test_github_helpers_and_state_persistence` (`test_pr_automation.py:558-595`) and
  `test_state_is_persisted_against_the_issue_subject` (`test_issue_automation.py:290-295`),
  which today reach it with a faked `subprocess.run` and would otherwise resolve Forgejo
  from the tenant's own `.vibey-gh.toml` and open `https://forgejo.local/…`.
- [ ] `grep -c 'monkeypatch.setattr' test/test_pr_automation.py test/test_issue_automation.py`
  is lower than before, file by file.
- [ ] Whole suite green at 100% line and branch coverage; black, isort, mypy and ruff clean.

## Tests to write first (TDD)
- `test/test_github_state_forge.py` (new, `RecordingForge`):
  `test_a_new_state_comment_goes_to_the_change_request_or_the_issue` (subject `"pr"` →
  `comment_on_change_request`; `"issue"` → `comment_on_issue`; both after
  `("for_repository", "o/r")`); `test_an_existing_state_comment_is_updated_in_place`
  (`update_comment` receives the matching comment dict); `test_a_refused_write_raises_with_
  the_callers_prefix` (`update_comment=(False, "comment has no ID")` → `"could not persist
  automation state: comment has no ID"`); `test_a_repository_the_forge_cannot_name_is_an_
  error` (`repository_name=("", "no repository")` → raises `"no repository"`, and no write
  verb is called).
- `test/test_gh_transport.py:471-531`: add `forge=GitHubForge(root=workdir)` to each
  `github_state.upsert_comment(...)` "after" lambda (`Before.*` and every expectation stay).
- `test/test_pr_automation.py:558-595` (`test_github_helpers_and_state_persistence`): stop
  patching `subprocess.run`; use the conftest `fake_gh` fixture (scripted answers keyed by
  the joined argv) with `forge=GitHubForge(root=tmp_path)` passed to `pa.upsert_state`, and
  assert the same four command shapes (`pr comment`, `issues/comments/9`, `api graphql`, the
  `comment has no ID` refusal), the `GH_REPO=explicit/repository` case, and the failing
  write (`match="persist"`). The two `pa._gh_json(...)` assertions (568, 591-592) stay but
  now run through `fake_gh` too (the alias is deleted in child lane 2).
- `test/test_issue_automation.py:290-295`: same conversion — `fake_gh`,
  `forge=GitHubForge(root=tmp_path)`, assert the first invocation's argv starts
  `issue comment 55 --repo o/r`.

## Checks the lane must run (all must pass)
```bash
cd src/vibey_tools/gh
python -m pytest -q --no-cov test/test_github_state_forge.py test/test_gh_transport.py test/test_pr_automation.py test/test_issue_automation.py test/test_conversation.py test/test_gh_cli.py test/test_forge_snapshot.py
python -c "import vibey_gh.github_state, vibey_gh.pr_automation, vibey_gh.issue_automation, vibey_gh.forge_selector"
python -m pytest -q
python -m black --line-length 100 --check vibey_gh test
isort --check-only vibey_gh test
python -m mypy vibey_gh
cd ../../..
UV_CACHE_DIR=$TMPDIR/uvcache uv run ruff check src/vibey_tools/gh
UV_CACHE_DIR=$TMPDIR/uvcache uv run ruff format --check src/vibey_tools/gh
git diff --stat
```
Nothing here is platform-specific: the same commands prove the change on macOS (the lane
host) and on Linux (CI's `tools` job), per 8.h.

## Out of scope
Everything else in `pr_automation.py` (child lanes 2 and 3), `install.py` (child lane 4),
`issue_automation.py` beyond `upsert_state` and `conversation.py` (#343), `cli.py`,
`forge_snapshot.py`, `delivery_sources.py`, adapter files, `test/conftest.py`, and this
repository's `.vibey-gh.toml`. Do not edit CHANGELOG.md, docs/, ADRs, CLAUDE.md, AGENTS.md,
GEMINI.md or skill trees -- the docs wave owns those. Do not push, open PRs, or change git
remotes. Commit locally with a Conventional Commit message when done.

## Conventions this lane relies on (everything needed is here)
**C1 — the adapter contract.** Every adapter verb answers `(value, problem)`. `problem` is
`""` exactly when the forge answered; otherwise `value` is the empty value for its type and
`problem` is one sentence. No verb raises for a failed call, a missing client, a non-2xx
status or unreadable output (`vibey_gh/interfaces/forge_adapter_interface.py:9-16`). A verb
a forge has no equivalent for answers `(<empty>, NotSupported(kind, verb, reason).problem)`,
text `f"{kind.value} does not support {verb}: {reason}"`; the caller reports it like any
other problem (10.f).

**C5 — comments are mappings.** A `Comment` is `{"id": str, "url": str, "body": str,
"author": {"login": str}, "createdAt": str}`; GitHub comments may also carry `databaseId`;
GitLab adds `noteable_type` and `noteable_iid`. `update_comment` takes the comment dict as
found in `comments`, because each forge needs a different part of it to edit it.

**C6 — how this module calls the adapter.** Obtain with
`forge = ForgeSelector().resolve(forge)` (#332: the injected forge, else
`select(load_config())`); bind where today's code called `github_state.repository()`:
`name, problem = forge.repository_name()`; `if problem: raise RuntimeError(problem)`;
`bound = forge.for_repository(name)` — once per call.

**C7 — tests (amended for 9.b and the fakes standard).**
- 100% line and branch coverage of `vibey_gh` (`src/vibey_tools/gh/pyproject.toml:64-71`).
  Focused runs need `--no-cov`.
- Substitute only at a declared seam: pass `forge=`. Never `monkeypatch.setattr` a module
  or class attribute (`subprocess.run`, `github_state.gh_json`, a module function), never
  `mock.patch` or `MagicMock`. `monkeypatch.setenv/delenv/chdir` are fine.
- `RecordingForge` (#332): each scripted verb returns its value (or calls it, if callable);
  `repository_name()` defaults to `("o/r", "")`; `for_repository(name)` records and returns
  itself; `.calls` lists `(verb, *args, *sorted(kwargs.items()))`; an unscripted verb raises
  `AttributeError`.
- GitHub argv proofs: the conftest `fake_gh` fixture (`test/conftest.py:87-156`; a real `gh`
  executable first on PATH; `script({...})` keyed by the space-joined argv; `invocations()`
  with argv, cwd and stdin — use it rather than `calls()` for a multi-line `--body`).
- No test leaves the machine unless marked `network` (`test/conftest.py:22-40`). Never touch
  `test/conftest.py`.

**C8 — the formatter trap.** Checked by both `black --line-length 100` + `isort` and the root
`ruff format`. Keep lines ≤ 100 columns (≤ 95 with nested calls); bind long comparisons to a
local before asserting; no implicit string concatenation that would fit on one line; one
argument per line with a trailing comma in multi-line calls; no backslash continuations.

## Hard repository rules (always)
- domain/ stays pure (no I/O, async, clock, network); enforced by tests/domain/test_domain_purity.py.
- Dependencies point inward: domain -> application -> infrastructure -> cli (import-linter).
- CreditsExhausted never has resets_at. A capacity rejection outranks a completion claim.
- Code lives in classes with an interface beside each (ADR-0016); module functions need a written reason.
- Every job idempotent under replay; the ledger is append-only.

## Proposed child lanes
### 1. feat(gh): the durable state comment reaches the forge only through the adapter (this issue)
`github_state.upsert_comment`, plus the `forge` pass-through on the two `upsert_state`
functions, as specified above. Depends on #332, #335, #336 and the operator's `[platform]`
declaration.

### 2. feat(gh): PR automation reads, readies, labels and records through the adapter
`vibey_gh/pr_automation.py` except `mirror_fork`: delete `_gh_json = github_state.gh_json`
(469); `fetch_pr(number, *, forge=None)` → `forge.change_request_with_thread(number)`, raise
on problem; `evaluate_pr(number, head_sha, cfg, *, forge=None)` and `ready_draft(...)`
resolve with `cfg` and pass `forge`; `ready_draft`'s `gh pr ready` (513-518) →
`forge.mark_ready(number)`, not ok → `RuntimeError(f"could not mark PR ready: {problem}")`;
`record(number, payload, kind, *, forge=None)` passes `forge` to `fetch_pr` and
`upsert_state`; `exhausted_pull_requests(cfg, *, forge=None)` binds and calls
`bound.labelled_open_change_request_numbers(label=EXHAUSTED_LABEL)`, raising on problem;
`self_heal(number, cfg, *, forge=None)` threads `forge` and binds for
`bound.remove_label(number, EXHAUSTED_LABEL)` (result ignored); `ensure_labels(*,
forge=None)` → `forge.create_label(name, colour=…, description=…, update_existing=True)`
per definition. Tests in `test/test_pr_automation.py`: every test that patches
`pa.fetch_pr`, `pa.upsert_state`, `pa._gh_json` (including 603, which the original issue
missed) or `subprocess.run` for these functions (375-434, 558-595, 602-657, 954-988) passes
a `RecordingForge` (or `fake_gh` + `GitHubForge` for argv proofs) instead; new tests
`test_ready_draft_raises_the_forges_problem`, `test_self_heal_binds_before_removing_the_
label`, `test_exhausted_listing_raises_when_the_forge_cannot_list`,
`test_ensure_labels_updates_existing_definitions`. `test/test_gh_cli.py`'s pr-automation
tests replace functions by name and pass unmodified. Depends on child 1, #333
(`change_request_with_thread`), #334 (`labelled_open_change_request_numbers`), #335.

### 3. feat(gh): the fork mirror clones from the forge's own URL through the adapter
`pr_automation.mirror_fork(number, cfg, *, forge=None)` (676-761): unchanged checks;
`url, problem = forge.fork_clone_url(owner, repo)` (raise on problem) replaces the
hard-coded `https://github.com/{owner}/{repo}.git` (688) in `git fetch --quiet <url>
<head>`; `replacement, problem = forge.create_change_request(base=str(pr["baseRefName"]),
head=branch, title=f"Repair #{number}: {title}", body=body)`, `None` →
`RuntimeError(f"could not open replacement pull request: {problem}")`; then
`forge.add_label(replacement, EXTERNAL_REPAIR_LABEL)`,
`forge.comment_on_change_request(number, <same text>)`,
`forge.close_change_request(number)` — unbound, as today. Tests:
`test_mirror_fork_success_and_failures` (659-700) injects a `RecordingForge` for the
forge verbs (the `git fetch`/`git push` calls stay on the existing `subprocess.run` fake
until a git seam exists — no new patch); new `test_mirror_fork_uses_the_forges_clone_url_
and_verbs`. Depends on child 2 (same file) and #335 (`fork_clone_url`,
`create_change_request`, `add_label`, `close_change_request`).

### 4. feat(gh): install notices list secrets through the adapter
`install.installation_notices(*, forge=None)` (`install.py:465-487`): `names, problem =
ForgeSelector().resolve(forge).secret_names()`; on problem return `(*notices, f"could not
list repository secrets ({problem}); skipping secret/permission checks")` (D3: was `"gh not
found; …"` for a missing `gh` and silence for any other failure); otherwise today's
"configure repository secret" notices. `cli._install(args, resolver=None)` gains
`forge=None` and passes it on (the declared seam `_install` already uses for its resolver).
Tests: `test_installation_notices_report_missing_secrets` / `…_degrade_when_gh_is_not_
installed` (`test_pr_automation.py:162-187`) inject `RecordingForge(secret_names=…)` instead
of patching `subprocess.run`; `test_install_finishes_when_gh_is_not_on_path` (190-214)
writes `[platform]\nkind = "github"\n` into its repository's `.vibey-gh.toml`; and every
other `main(["install"])` test — `test/test_gh_cli.py` 60, 71, 104, 114, 139, 437 and
`test/test_marketplace.py:376` (their `repo` fixtures declare no `[platform]`) — must not
reach `forgejo.local`: declare GitHub in the fixture's config and put the conftest `fake_gh`
on PATH (no answers → the new "could not list repository secrets" notice). Depends on
#332 and #337 (`secret_names`).
