<!-- audit: SUPERSEDED — replaced by ADR-0046 §3/§10 `adapter.py` bound to the routed engine, plus `LoopSelector`/`SelectingLoopProvider`/`LoopRoutingPort` and `EngineQueueSaturated` in application/ -->
Closing as superseded by draft ADR-0046 (two loops). **Nothing from this issue was merged.**

`LoopServiceAdapter` here publishes to one engine's own service. Under sub-doctrine 8.c as ratified by #392 (`doctrines.md:196-234`), ADR-0046 §2–§3 put the outer rotation layer in vibey (`LoopSelector`, `SelectingLoopProvider`, `LoopRoutingPort`, `RoutedAdapterBinderInterface`) and bind the adapter to the engine the loop routed to *before* `start`, so `EngineAdapter.descriptor` is known exactly as `build_implement_handler.py:185` needs (§3, "Flow for a BUILD job", steps 1–5).

Carried over into ADR-0046 (`infrastructure/loop_service/adapter.py` and the application row of §10):
- the evidence path is unchanged: `tail` reads `events.jsonl` through R20's `RunDirTailer` (#367, kept; ADR-0046 non-negotiable 5), so `run_and_record` and the capacity-before-completion order (`build_implement_handler.py:241-263`) are untouched;
- `RunSpec.supersede_key`/`attempt` (defaulted fields) feed the supersede key, now fenced across both loops by §6's worktree fence;
- `EngineQueueSaturated` in `application/worker.py` → `Defer(capacity=False)`: no circuit opens, no attempt is spent, and it **never** triggers a paid fallback (§5 table, "queue saturation" row);
- `run_exit_code` from `RunResult`, else the persisted result file; preflight and `help_text` from probes (now residency-aware, §4);
- the stop rule for existing tests that compare whole `RunSpec` values.

Evidence: `specs/ADR-two-loops.md` §2, §3, §5, §10. ADR-0046's lanes are still to be filed.
