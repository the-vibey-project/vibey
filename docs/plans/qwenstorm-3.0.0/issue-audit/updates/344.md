<!-- audit: TOO-BIG — two unrelated modules (rulesets; flatten, 1372 lines, whose 1994-line test file constructs `Flattener()` 95 times) in one lane; also STALE: the flatten fixture would patch `flatten.ForgeSelector.resolve` (9.b/D7), `test_a_request_body_is_actually_sent` calls the deleted `rs._api`, "Part 1 block" not inlined -->
<!-- title: feat(gh): ruleset reconciliation reaches the forge only through the adapter -->
Part of the **QwenStorm for 3.0.0**. A local sovereign lane (qwenloop until ADR-0046's rename lands) implements this issue; the result is reviewed and verified before it becomes a pull request.

## Why
Sub-doctrine 8.b (`src/vibey_tools/gh/docs/doctrines.md:138-139`) makes self-hosted Forgejo
the default forge, with GitHub and GitLab declared-only; `[platform] kind` defaults to
`forgejo` (`vibey_gh/config.py:288`). `vibey_gh/rulesets.py` reconciles branch rulesets
through a private `gh api` runner, `_api` (`rulesets.py:179-196`), called by
`fetch_ruleset` (198-209), `create_ruleset` (212-213) and `update_ruleset` (216-222), each
naming the repository with `github_state.repository()`. It ignores `[platform]`.

A rule a forge cannot express must come back as a problem, never be dropped:
`reconcile_one` already refuses to skip silently (`rulesets.py:226-230`), and #338 makes the
Forgejo adapter refuse what `branch_protections` cannot express with a NotSupported
sentence. The default `[rulesets]` policy asks for rules Forgejo refuses (merge queue,
linear history, review-thread resolution, code-owner review, bypass actors), so on the
sovereign default `vibey-gh rulesets` fails loudly until the operator chooses an expressible
policy — honest (10.f), and this lane must keep it that way.

When a paid forge is declared, 8.b says it "relays through the sovereign host rather than
replacing it" (`doctrines.md:179-184`). That relay belongs behind `ForgeAdapterInterface`;
this lane keeps every call on the interface.

This issue was split: it now covers `rulesets.py` only. `flatten.py`'s review-thread walk is
child lane 2 below.

## Depends on
- **Operator prerequisite (not lane work):** this repository's root `.vibey-gh.toml`
  declares `[platform]` / `kind = "github"` on `develop`. Today it has no `[platform]` table
  on the integration branch (`cce648ef`) or `origin/develop` (`e9e6c79f`); the declaration
  exists only as commit `b60dad4c` on the local branch `docs/changelog-2.1.0`. 8.b requires a
  human to write it (`doctrines.md:179-181`); the lane must not.
- #332 (forge-0a): `ForgeSelector.resolve`, `repository_name`, `for_repository`,
  `NotSupported`, `RecordingForge` (`test/forge_doubles.py`).
- #338 (forge-0g): `branch_rules(name, branch)`, `create_branch_rules(document)`,
  `update_branch_rules(rules_id, document)`. On GitHub they run today's argv
  (`api repos/<R>/rulesets`, `api repos/<R>/rulesets/<id>`, `… --method POST|PUT --input -`
  with the JSON on stdin) and answer today's failure text
  (`"gh api repos/<R>/rulesets --method POST: <stderr>"`).

## Required behaviour
1. Delete `_api` (179-196) and the imports that become unused (`json`, `subprocess`,
   `cast`, `from vibey_gh import github_state`).
2. `fetch_ruleset(name, branch, *, forge=None)`: `forge = ForgeSelector().resolve(forge)`;
   bind (C6.2, raise on problem); `document, problem = bound.branch_rules(name, branch)`;
   raise `RuntimeError(problem)` on problem; return `document` (`None` when no ruleset has
   that name). Keep the docstring's reason for re-fetching by ID (the GitHub adapter does
   it now) and add that `branch` is what a per-branch forge keys its protection by.
3. `create_ruleset(payload, *, forge=None)`: resolve; bind; `ok, problem =
   bound.create_branch_rules(payload)`; not ok → `RuntimeError(problem)`.
4. `update_ruleset(ruleset_id, payload, *, forge=None)`: resolve; bind; `ok, problem =
   bound.update_branch_rules(str(ruleset_id), payload)`; not ok → `RuntimeError(problem)`.
5. `reconcile_one(branch, policy, *, dry_run=False, forge=None)`: resolve once; call
   `fetch_ruleset(name, branch, forge=forge)` and pass `forge=forge` to
   `create_ruleset`/`update_ruleset`. `reconcile(cfg, *, dry_run=False, forge=None)`
   resolves with `cfg` once, after the `enabled` early return, and passes it on.
6. The pure half (`ruleset_name`, `desired_rules`, `bypass_actor_payload`,
   `build_ruleset`, `diff_ruleset`, …) is untouched.
7. On GitHub the argv and stdin are unchanged byte for byte; deliberate differences: **D1**
   `gh` runs in `load_config().root` (or the resolved `cfg.root`) instead of the process's
   working directory (same repository); **D5** a missing `gh` or unreadable output raises
   `RuntimeError(problem)`.
8. The five functions stay module-level; each carries a one-line reason at its definition
   (9.b, `doctrines.md:349`), e.g. `# Module-level: cli.py and its tests address these by
   name; the module converges on a class in its own lane (ADR-0016).`

## Where to change
`src/vibey_tools/gh/vibey_gh/rulesets.py` only. Imports:
`from vibey_gh.forge_selector import ForgeSelector`,
`from vibey_gh.interfaces.forge_adapter_interface import ForgeAdapterInterface`.
Tests: `test/test_rulesets.py`.

## Acceptance criteria
- [ ] `grep -nE '"gh"|gh_json|github_state|subprocess|_api\(' vibey_gh/rulesets.py` finds
  nothing.
- [ ] `grep -nE 'rs\._api|setattr\(rs|setattr\(subprocess' test/test_rulesets.py` finds
  nothing, and `grep -c 'monkeypatch.setattr' test/test_rulesets.py` is lower than 22 (its
  count at `cce648ef`).
- [ ] `test_fetch_ruleset_end_to_end_through_the_real_gh_adapter` and
  `test_a_request_body_is_actually_sent` still prove, through a real `gh` executable on
  PATH, that a read sends no body and a write sends `--input -` with the payload on stdin.
- [ ] `test/test_gh_cli.py`'s rulesets tests pass unmodified.
- [ ] Whole suite green at 100% line and branch coverage; black, isort, mypy and ruff clean.

## Tests to write first (TDD)
In `test/test_rulesets.py` (`from forge_doubles import RecordingForge`,
`from vibey_gh.forge_github import GitHubForge`):
- New: `test_rulesets_bind_and_use_branch_rule_verbs` (`RecordingForge`: `reconcile_one`
  on a drifted ruleset records `("for_repository", "o/r")`, `("branch_rules", "vibey-gh:
  develop", "develop")` and `("update_branch_rules", "9", <payload>)`);
  `test_a_forge_that_refuses_the_rules_raises_its_problem`
  (`create_branch_rules=(False, "forgejo does not support branch rules: no equivalent for
  merge_queue")` → `pytest.raises(RuntimeError, match="does not support branch rules")`);
  `test_reconcile_one_passes_the_branch_to_the_fetch`.
- Delete `test_api_raises_with_the_apis_own_reason_on_failure` and
  `test_api_returns_none_for_an_empty_body` (441-449): their runner is gone, and #338 pins
  the same failure text on the GitHub verb.
- `fake_gh` argv proofs, with `monkeypatch.setenv("GH_REPO", "o/r")` and
  `forge=GitHubForge(root=tmp_path)` (no `subprocess.run` patch):
  `test_fetch_ruleset_refetches_the_named_entry_by_id_for_its_rules` (452-466) and
  `test_fetch_ruleset_end_to_end_through_the_real_gh_adapter` (469-483) script
  `api repos/o/r/rulesets` and `api repos/o/r/rulesets/2`;
  `test_fetch_ruleset_returns_none_when_no_ruleset_has_that_name` (485-489) scripts only
  the listing; `test_create_and_update_ruleset_post_the_payload_as_json` (491-505) and
  `test_a_request_body_is_actually_sent` (596-619, keep its docstring) script
  `api repos/o/r/rulesets --method POST --input -` and
  `api repos/o/r/rulesets/7 --method PUT --input -` with `"read_stdin": true` and assert
  `json.loads(invocation["stdin"]) == {"name": "x"}`, and that the read's argv has no
  `--input`.
- Orchestration (508-594): pass `forge=RecordingForge(...)` instead of patching
  `fetch_ruleset`/`create_ruleset`/`update_ruleset`/`reconcile_one`:
  `branch_rules=(None, "")` or `({**desired, "id": 9}, "")`,
  `create_branch_rules=(True, "")` / `(False, "422 required status check contexts must be
  unique")`, `update_branch_rules=(True, "")`. A test that asserts a verb is never called
  scripts nothing for it (an unscripted verb raises `AttributeError`).
  `test_reconcile_covers_the_integration_and_release_branches` scripts `branch_rules` as a
  callable that returns `build_ruleset(branch, <that branch's expected policy>) | {"id":
  branch}`, runs `rs.reconcile(cfg, dry_run=True, forge=forge)`, and asserts both outcomes
  are unchanged — which holds only if each branch was reconciled with its own policy.
  `test_reconcile_is_a_noop_when_disabled` passes `RecordingForge()` and asserts
  `.calls == []`.

## Checks the lane must run (all must pass)
```bash
cd src/vibey_tools/gh
python -m pytest -q --no-cov test/test_rulesets.py test/test_gh_cli.py
python -m pytest -q
python -m black --line-length 100 --check vibey_gh test
isort --check-only vibey_gh test
python -m mypy vibey_gh
cd ../../..
UV_CACHE_DIR=$TMPDIR/uvcache uv run ruff check src/vibey_tools/gh
UV_CACHE_DIR=$TMPDIR/uvcache uv run ruff format --check src/vibey_tools/gh
git diff --stat
```
Nothing here is platform-specific: the same commands prove the change on macOS (the lane
host) and on Linux (CI's `tools` job), per 8.h.

## Out of scope
`flatten.py` (child lane 2), `reconcile.py` (#341), `cli.py`, adapter files,
`test/conftest.py`, and this repository's `.vibey-gh.toml`. Do not edit CHANGELOG.md,
docs/, ADRs, CLAUDE.md, AGENTS.md, GEMINI.md or skill trees -- the docs wave owns those. Do
not push, open PRs, or change git remotes. Commit locally with a Conventional Commit message
when done.

## Conventions this lane relies on (everything needed is here)
**C1 — the adapter contract.** Every adapter verb answers `(value, problem)`. `problem` is
`""` exactly when the forge answered; otherwise `value` is the empty value for its type and
`problem` is one sentence. No verb raises for a failed call, a missing client, a non-2xx
status or unreadable output (`vibey_gh/interfaces/forge_adapter_interface.py:9-16`). A verb
a forge has no equivalent for answers `(<empty>, NotSupported(kind, verb, reason).problem)`,
text `f"{kind.value} does not support {verb}: {reason}"`; the caller reports it like any
other problem (10.f), never special-cases a forge, never skips silently.

**C5 — branch rules are the ruleset document.** `branch_rules` answers the document
`build_ruleset` builds (`rulesets.py:122-131`) plus `"id"` (a string branch name on
Forgejo), so `diff_ruleset` is unchanged.

**C6 — how this module calls the adapter.** Each public function gains a keyword-only last
parameter `forge: ForgeAdapterInterface | None = None` and resolves with
`ForgeSelector().resolve(forge, cfg)` (or `.resolve(forge)` without a `cfg`; #332 returns the
injected forge, else `select(cfg)`, else `select(load_config())`). Bind where today's code
called `github_state.repository()`: `name, problem = forge.repository_name()`;
`if problem: raise RuntimeError(problem)`; `bound = forge.for_repository(name)`.

**C7 — tests (amended for 9.b and the fakes standard).**
- 100% line and branch coverage of `vibey_gh` (`src/vibey_tools/gh/pyproject.toml:64-71`).
  Focused runs need `--no-cov`.
- Substitute only at a declared seam: pass `forge=`. Never `monkeypatch.setattr` a module
  or class attribute (`rs._api`, `rs.fetch_ruleset`, `subprocess.run`), never `mock.patch`
  or `MagicMock`. `monkeypatch.setenv/delenv/chdir` are fine.
- `RecordingForge` (#332): each scripted verb returns its value (or calls it, if callable);
  `repository_name()` defaults to `("o/r", "")`; `for_repository(name)` records and returns
  itself; `.calls` lists `(verb, *args, *sorted(kwargs.items()))`; an unscripted verb raises
  `AttributeError`.
- GitHub argv proofs: the conftest `fake_gh` fixture (`test/conftest.py:87-156`; a real `gh`
  executable first on PATH; `script({...})` keyed by the space-joined argv, with
  `"read_stdin": true` to capture a body; `invocations()` with argv, cwd and stdin).
- No test leaves the machine unless marked `network` (`test/conftest.py:22-40`). Never touch
  `test/conftest.py`.

**C8 — the formatter trap.** Checked by both `black --line-length 100` + `isort` and the root
`ruff format`. Keep lines ≤ 100 columns (≤ 95 with nested calls); bind long comparisons to a
local before asserting; no implicit string concatenation that would fit on one line; one
argument per line with a trailing comma in multi-line calls; no backslash continuations.

## Hard repository rules (always)
- domain/ stays pure (no I/O, async, clock, network); enforced by tests/domain/test_domain_purity.py.
- Dependencies point inward: domain -> application -> infrastructure -> cli (import-linter).
- CreditsExhausted never has resets_at. A capacity rejection outranks a completion claim.
- Code lives in classes with an interface beside each (ADR-0016); module functions need a written reason.
- Every job idempotent under replay; the ledger is append-only.

## Proposed child lanes
### 1. feat(gh): ruleset reconciliation reaches the forge only through the adapter (this issue)
`rulesets.py` and `test/test_rulesets.py`, as specified above. Depends on #332, #338 and
the operator's `[platform]` declaration.

### 2. feat(gh): flatten asks the forge adapter for unresolved review threads
`vibey_gh/flatten.py`: `Flattener.__init__(self, forge: ForgeAdapterInterface | None =
None)` stores it; `_review_threads` (609-752) becomes an instance method
`_review_threads(self, cfg, branch)`, its one caller (288) passes `cfg`. The walk resolves
`forge = ForgeSelector().resolve(self._forge, cfg)`; before the first page `name, problem =
forge.repository_name()` (problem → `return None, (), problem`) and `forge =
forge.for_repository(name)`; each page `payload, problem = forge.review_thread_page(branch,
cursor)` (problem → `return number, threads, problem`). Everything after — `errors`,
`data.repository.pullRequests`, siblings, `pageInfo`, `_THREAD_PAGES` and every early
return — is unchanged. Delete `_THREADS_QUERY`/`_THREADS_PER_PAGE` (131, 152-169; now
`forge_github.REVIEW_THREADS_QUERY`/`…_PER_PAGE`), the `try/except` around the old call and
the `github_state` import; rewrite the docstring paragraph at 645-649 (the forge adapter is
asked). Declared GitHub difference: a failing `gh api graphql` now reads "`gh api graphql`
failed: <last stderr line>" (the transport's text) instead of the whole argv. `cli._flatten
(args)` (`cli.py:421-438`) gains the declared seam `flattener: FlattenerInterface | None =
None` (as `_install` has `resolver`). Tests in `test/test_flatten.py` (1994 lines): the
autouse `forge` fixture (171-182) stops patching `github_state.gh_json`; the `Forge` double
(84-160) gains `repository_name()` → `("the-vibey-project/vibey", "")`, `for_repository`
→ itself, and `review_thread_page(head_ref, cursor)` recording `("review_thread_page",
head_ref, *([f"after={cursor}"] if cursor else []))` and converting its scripted replies
the way flatten used to (`FileNotFoundError` → "the GitHub CLI (`gh`) is not installed";
`OSError`/`RuntimeError`/`ValueError`/`LookupError`/`TypeError` → "`gh api graphql`
failed: …"; a dict → `(reply, "")`); the fixture also records the fake in a module-level
list, and one checked scripted replacement turns every `Flattener()` construction (95, plus
the four subclasses at 1828-1874) into `Flattener(_current_forge())`; the flatten CLI tests
(1945-1991) that reach the forge call `cli._flatten(argparse.Namespace(...),
flattener=flatten.Flattener(forge))`, while the refusal test keeps `cli_main` (it refuses
before any forge call). No `monkeypatch.setattr` of `ForgeSelector.resolve` or any module
attribute. Every `plan.threads_problem` assertion passes unchanged; add
`test_a_repository_the_forge_cannot_name_is_reported`. Depends on #332 and #336
(`review_thread_page`, `REVIEW_THREADS_QUERY`).
