<!-- audit: TOO-BIG — ten production files across all four layers (domain config + interface + deployment, application handler + port, infrastructure state repo + config loader + az adapter, bootstrap) and seven independent behaviours; line refs still hold at 739536ea except 8.b's cloud line (now doctrines.md:136-137), and its application tests copy FakeSpecStore/FakeConsentStore, which lane fakes-deploy deletes -->
<!-- title: feat(config)!: [deploy].target names a cloud with an adapter, and [deploy].iac follows it -->
Part of the **QwenStorm for 3.0.0**. A local sovereign lane (qwenloop until ADR-0046's rename lands) implements this issue; the result is reviewed and verified before it becomes a pull request.

This issue is **child lane 1 of 4** of "the deployment scope's provider follows [deploy].target"
(see *Proposed child lanes* at the end). It changes only the pure `[deploy]` configuration: its
defaults, its validation and a reader for a project's stored config. Nothing reads `[deploy]` at
runtime yet; child lane 3 wires it in.

## Why
Sub-doctrine 8.b (`src/vibey_tools/gh/docs/doctrines.md:136-137` at `739536ea`) makes self-hosted
OpenStack the cloud default and Azure, AWS and GCP declared-only. ADR-0042
(`docs/architecture/decisions/0042-sovereign-self-hosted-defaults-and-declared-paid-relays.md:58-64`)
sets `deploy.target` to default to `openstack`. The configuration does not hold that shape:
- `DeployConfig` (`src/vibey/domain/config.py:136-140`) defaults `iac` to `"bicep"`, an Azure
  format, even for the OpenStack target.
- `_parse_deploy` (`config.py:453-459`) accepts any string for `target` and `iac`, so
  `target = "aws"` or `target = "openstack"` with `iac = "bicep"` loads without complaint, and a
  typo in the one declaration that selects a paid cloud passes unseen.
- No reader turns a project's stored config (a plain mapping) into a `DeployConfig`, which child
  lane 3 needs.

Only two targets have an adapter today: `openstack` (the sovereign default; its real CLI adapter is
#331) and `azure` (`src/vibey/infrastructure/azure/az_cli.py`). 8.b names **AWS** as the default
paid cloud (doctrines.md:187-193), but no AWS adapter exists, so `aws` is refused rather than
accepted and silently deployed nowhere. When an AWS adapter lands, it joins the map below. Nothing
here makes Azure the default paid cloud: it is only the one paid target with an adapter.

## Required behaviour
1. `DeployConfig()` equals `DeployConfig(enabled=False, target="openstack", iac="heat")`.
2. `[deploy].target` must be a key of `DEPLOY_IAC_BY_TARGET`, which means `"openstack"` or
   `"azure"`. Any other value raises
   `ConfigError("deploy.target", f"must be one of {', '.join(sorted(DEPLOY_IAC_BY_TARGET))} (the targets with an adapter), got {target!r}")`.
3. `[deploy].iac` defaults to the first format listed for the target, and must be one of the
   target's formats: `openstack -> ("heat",)` and `azure -> ("bicep", "arm")`. Any other value
   raises `ConfigError("deploy.iac", f"target {target!r} runs {', '.join(formats)}, got {iac!r}")`.
   `target = "azure"` with no `iac` gives `"bicep"`, exactly as before.
4. `DeployConfig.from_mapping(config)` returns the `[deploy]` table from a mapping shaped like a
   parsed vibey.toml or a project's stored config, validated as in 2 and 3. An empty mapping `{}`
   gives the defaults. A `deploy` value that is not a table raises `ConfigError("deploy", ...)`
   (the existing `_optional` message).
5. `DEPLOY_REGION_BY_TARGET = {"openstack": "RegionOne", "azure": "eastus"}` is declared beside
   `DEPLOY_IAC_BY_TARGET` (12.c: data, not a constant buried in a handler). Child lane 3 reads it
   for a synthesized spec's default region; this lane only declares it and pins it in a test.
6. A new interface `DeployConfigInterface` (read-only `enabled: bool`, `target: str`, `iac: str`)
   sits in `src/vibey/domain/interfaces/config_interface.py` and is exported from
   `src/vibey/domain/interfaces/__init__.py`; `DeployConfig()` satisfies it.
7. Everything else in `config.py` is unchanged: `tests/domain/test_config.py` (whose example
   declares `target = "azure"`, `iac = "bicep"`, `:50-54`, asserted at `:107-108`) passes unedited,
   and domain purity holds (`tests/domain/test_domain_purity.py`).

## Where to change
Line numbers are from the storm integration branch at `739536ea`; if one has moved, find the quoted
text. Use `edit_file` for every existing file (`config.py` is over 700 lines).

- `src/vibey/domain/config.py`
  - Imports (lines 9-11): add `from collections.abc import Mapping`.
  - After line 35 (`DEFAULT_LOCAL_CONTEXT_WINDOW = 32_768`), add:
    ```python
    # Deploy targets with an adapter (ADR-0042, sub-doctrine 8.b) -> the IaC formats that
    # adapter runs; the first entry is the target's default `iac`. A declared-only cloud
    # with no adapter yet (8.b's paid default, AWS, among them) is refused, not accepted
    # and deployed nowhere; it joins this map with its adapter.
    DEPLOY_IAC_BY_TARGET: dict[str, tuple[str, ...]] = {
        "openstack": ("heat",),
        "azure": ("bicep", "arm"),
    }
    # The region a synthesized spec uses when the deploy interview names none.
    DEPLOY_REGION_BY_TARGET: dict[str, str] = {"openstack": "RegionOne", "azure": "eastus"}
    ```
  - `DeployConfig` (lines 136-140): change the default to `iac: str = "heat"`, and add:
    ```python
    @classmethod
    def from_mapping(cls, config: Mapping[str, object]) -> "DeployConfig":
        """The [deploy] table of a parsed vibey.toml or a project's stored config."""
        return _parse_deploy(dict(config))
    ```
  - `_parse_deploy` (lines 453-459) does the following, in order:
    1. `table = _optional(data, "deploy", "deploy", dict, {})` (as today).
    2. `target = _optional(table, "target", "deploy.target", str, "openstack")`.
    3. `formats = DEPLOY_IAC_BY_TARGET.get(target)`. If it is `None`, raise the Required behaviour 2
       error.
    4. `iac = _optional(table, "iac", "deploy.iac", str, formats[0])`.
    5. If `iac not in formats`, raise the Required behaviour 3 error.
    6. Return `DeployConfig(enabled=_optional(table, "enabled", "deploy.enabled", bool, False), target=target, iac=iac)`.
- `src/vibey/domain/interfaces/config_interface.py`: add `DeployConfigInterface`, copying the layout
  of `TelemetryConfigInterface` (lines 30-36): `@runtime_checkable`, `Protocol`, three read-only
  `@property` members.
- `src/vibey/domain/interfaces/__init__.py`: add `DeployConfigInterface` to the
  `config_interface` import block (lines 3-7) and to `__all__`, keeping both sorted.
- New test file `tests/domain/test_deploy_target_config.py` (below). No other file changes.

## Acceptance criteria
- [ ] `uv run pytest -q -p no:cacheprovider tests/domain/test_deploy_target_config.py` passes (8 test functions, 9 cases).
- [ ] `uv run pytest -q -p no:cacheprovider tests/domain/test_config.py tests/domain/test_domain_purity.py tests/domain/test_deployment_domain.py`
      passes with those files unedited.
- [ ] `git diff --stat` names only the three source files above and the new test file.
- [ ] `src/vibey/domain/*` keeps 100% branch coverage.

## Tests to write first (TDD)
New `tests/domain/test_deploy_target_config.py` (pure domain: no database, no network, no marker,
no patching). Import `ConfigError`, `DeployConfig`, `DEPLOY_IAC_BY_TARGET`,
`DEPLOY_REGION_BY_TARGET` and `load_config_from_string` from `vibey.domain.config`, and
`DeployConfigInterface` from `vibey.domain.interfaces`.
- `test_default_deploy_config_is_sovereign`: `DeployConfig() == DeployConfig(enabled=False, target="openstack", iac="heat")`,
  and `load_config_from_string('[project]\nname = "x"\n').deploy == DeployConfig()`.
- `test_azure_target_defaults_its_iac_to_bicep`: `[deploy]\ntarget = "azure"\n` gives `iac == "bicep"`.
- `test_azure_accepts_arm`: `target = "azure"`, `iac = "arm"` loads with `iac == "arm"`.
- `test_unknown_target_is_refused`: `target = "aws"` raises `ConfigError` matching `deploy.target`
  and `the targets with an adapter`.
- `test_iac_must_match_the_target`: parametrized over (`openstack`, `bicep`) and (`azure`, `heat`);
  both raise `ConfigError` matching `deploy.iac`.
- `test_from_mapping_reads_a_stored_project_config`: `{"project": {"name": "p"}, "deploy": {"target": "azure"}}`
  gives target `"azure"` and iac `"bicep"`; `{}` gives `DeployConfig()`; `{"deploy": "azure"}` raises
  `ConfigError` whose `.path == "deploy"`.
- `test_every_target_declares_a_default_region_and_iac`: `set(DEPLOY_REGION_BY_TARGET) == set(DEPLOY_IAC_BY_TARGET)`,
  every formats tuple is non-empty, and `DEPLOY_REGION_BY_TARGET["openstack"] == "RegionOne"`.
- `test_deploy_config_satisfies_its_interface`: `isinstance(DeployConfig(), DeployConfigInterface)`.

## Checks the lane must run (all must pass)
    uv run ruff check . && uv run ruff format --check .
    uv run mypy --strict src/vibey
    uv run lint-imports
    uv run bandit -q -r src/vibey
    uv run pytest -q -p no:cacheprovider tests/domain
    uv run pytest -q -p no:cacheprovider --cov=vibey --cov-branch --cov-report=
    uv run coverage report --include='src/vibey/domain/*' --fail-under=100
    uv run coverage report --include='src/vibey/application/*' --fail-under=100
    uv run coverage report --include='src/vibey/infrastructure/*' --fail-under=100
    uv run coverage report --include='src/vibey/cli/*' --fail-under=100

One coverage run at a time. Until lane `fakes-harness-decouple` lands, `tests/conftest.py:146-156`
connects to PostgreSQL at session start, so the whole-suite coverage run needs it; the new test file
itself needs nothing and can run alone with `--noconftest -n 0`.

## Out of scope
- Child lanes 2-4 below: the scope digest, the spec builder and its persistence, the runtime
  config keys, bootstrap, and the az adapter's provider check. Do not touch
  `domain/deployment.py`, `application/`, `infrastructure/` or `bootstrap.py` here.
- An AWS or GCP adapter (a follow-up; 8.b's default paid cloud is AWS), and #331's OpenStack CLI
  adapter.
- Do not edit CHANGELOG.md, docs/, ADRs, CLAUDE.md, AGENTS.md, GEMINI.md or the skill trees; the
  docs wave owns them (for example, `docs/reference/configuration.md:228` still says the default is
  `"azure"`, and CLAUDE.md still speaks of "an opt-in Azure deployment stage set"). Do not push, open
  PRs, or change git remotes. Commit locally as
  `feat(config)!: [deploy].target names a cloud with an adapter, and [deploy].iac follows it`, with
  the footer `BREAKING CHANGE: [deploy].iac defaults to "heat" for the default openstack target (azure still defaults to "bicep"); a [deploy].target with no adapter, or an iac its target does not run, is refused.`

**Depends on:** nothing unmerged.

## Hard repository rules (always)
- domain/ stays pure (no I/O, async, clock, network); enforced by tests/domain/test_domain_purity.py.
- Dependencies point inward: domain -> application -> infrastructure -> cli (import-linter).
- CreditsExhausted never has resets_at. A capacity rejection outranks a completion claim.
- Code lives in classes with an interface beside each (ADR-0016); module functions need a written reason.
- Every job idempotent under replay; the ledger is append-only.
- Never edit tests/system/test_delivery_stage_set.py (protected path, .vibey-gh.toml:78-85).

## Proposed child lanes

### 1. feat(config)!: [deploy].target names a cloud with an adapter, and [deploy].iac follows it (this issue)
The spec above: `domain/config.py`, `domain/interfaces/config_interface.py`,
`domain/interfaces/__init__.py`, and `tests/domain/test_deploy_target_config.py`. Independently
mergeable: nothing reads `[deploy]` at runtime yet. Depends on nothing.

### 2. feat(deploy): an Azure scope keeps its pre-provider digest, and a spec must name a colon-free provider
Only `src/vibey/domain/deployment.py` and a new `tests/domain/test_scope_digest.py`. Since
c67495e4 (#319) `AzureTargetScope.digest()` (`deployment.py:24-28`) hashes
`provider:tenant:subscription:resource_group:environment`, so every consent recorded by 2.0.0 and
earlier (all of them Azure, form `tenant:subscription:resource_group:environment`) stopped matching.
Add `LEGACY_DIGEST_PROVIDER = "azure"` above the class with a comment saying pre-#319 consents hash
the 4-field form and all are Azure consents; `digest()` hashes the 4-field form when
`provider == "azure"` and the provider-first 5-field form otherwise (byte-identical to today's code
for every non-Azure provider). `validate()` (`:86-130`) adds `"provider is required"` for a blank
provider and `"<name> must not contain ':'"` for each of `provider`, `tenant_id`, `subscription_id`,
`resource_group`, `environment`, so the two forms differ structurally (3 separators against 4) and
consent cannot cross clouds. Tests (fields `tenant-123`, `sub-456`, `rg-vibey-dev`, `dev`, region
`eastus`): the Azure digest equals the literal `a3aca17c2ecbacc58a642eebf46c8698516c80b467e177e1bd5afa8c9cb94fe9`,
the OpenStack digest equals `57d727a9ffb8ec9eb505422ed834872e36ca66ee3072d0c8e110b69b66b66cb9`
(both recomputed with hashlib at audit time), `azure`/`openstack`/`aws` give three distinct
digests, a legacy consent matches the Azure spec only, and the two `validate()` errors
(parametrized over the five names). `tests/domain/test_deployment_domain.py` passes unedited.
Independently mergeable. Depends on nothing.

### 3. feat(deploy)!: the synthesized spec's provider, region and IaC follow [deploy].target, and survive a reload
`src/vibey/application/deploy_design_handler.py`: `build_deployment_spec` (`:36-76`) gains
keyword-only `deploy: DeployConfig | None = None` (`None` means `DeployConfig()`), sets
`provider=config.target` (an answer key `"provider"` is ignored: only the declaration picks the
cloud), `region=_get("region", DEPLOY_REGION_BY_TARGET[config.target])` (was `"eastus"`, `:59`) and
`iac_provider=_get("iac_provider", config.iac)` (was `"bicep"`, `:67`), with a provider-neutral
docstring (`:41-45`); `DeploySynthesizeHandler.__init__` (`:155-167`) takes and passes `deploy`
(call at `:185`). `src/vibey/infrastructure/deploy/state_repository.py:63-70`: `load_spec` restores
`provider=str(scope.get("provider", "azure"))` with a comment (a spec.json without the key predates
the field and was written by the Azure-only pipeline). `src/vibey/infrastructure/config_loader.py:10`:
`RUNTIME_CONFIG_KEYS = ("notifications", "telemetry", "deploy")`, docstring `:104-111` updated, and
the existing `parse_config` call (`:116-122`) validating the `[deploy]` table by child 1's rules before it is stored, so
`vibey new` (`cli/main.py:217`) stores it. `src/vibey/bootstrap.py`: import `DeployConfig` beside
`VibeyConfig` (`:77`); `build_full_worker` (`:341-352`) gains `deploy: DeployConfig | None = None`,
resolved beside `:371` as `DeployConfig.from_mapping(project.config)` when `None`, and passed to
`DeploySynthesizeHandler` (`:568`). Tests append to
`tests/application/test_deploy_design_and_acceptance.py` (spec follows an Azure target; default spec
is openstack/RegionOne/heat and validates; answers cannot switch the provider; the synthesize handler
stamps the configured target), using the shared `InMemoryDeploymentStateStore` from
`tests/fakes/deploy.py` if lane `fakes-deploy` has landed on the integration branch when this lane
starts, else the module's existing store fakes (`:531-545`) — adding no new private double and no
patch; to `tests/infrastructure/deploy/test_state_repository.py` (provider round-trips; a legacy
spec without `"provider"` loads as `"azure"` and a consent holding
`hashlib.sha256(b"t-1:s-1:rg-one:dev").hexdigest()` still matches); and to
`tests/infrastructure/test_config_loader.py` (a `[deploy]` table is stored; `target = "aws"` is
refused with `deploy.target`). `tests/system/test_delivery_stage_set.py` passes unedited. The commit
body says that a spec.json written between #319 and this change reads `"provider": "openstack"` even
for an Azure adopter, and must be re-synthesized (re-run DEPLOY_DESIGN) once child 4 lands. No SQL
is added anywhere (the state repository is file-based). Depends on children 1 and 2; rebase over
#321 (engines-pool), which also edits `bootstrap.py`.

### 4. feat(azure): the az adapter refuses a target scope for another provider
`src/vibey/application/azure_port.py:14-26`: add `class ScopeProviderMismatchError(Exception)`
("Raised when a cloud adapter is handed a target scope for another provider.") to the module and
`__all__`; like `MutationNotAuthorizedError` it carries no behaviour and gets no interface file.
`src/vibey/infrastructure/azure/az_cli.py`: a `_require_provider(scope)` method raising it with
`f"the az adapter deploys provider 'azure' only; this scope is {scope.provider!r}"`, called as the
first statement of `discover_environment` (`:64`), `execute_plan` (`:97-100`, on
`spec.target_scope`, before `_require_consent`), `get_resource_status` (`:145`) and `delete_resource`
(`:159-162`, before `_require_consent`), so it runs before any subprocess. The in-memory adapter
(`infrastructure/azure/adapter.py`) keeps accepting any provider. Tests in
`tests/infrastructure/azure/test_az_cli.py`: `_spec()` (`:34-55`) gains `provider="azure"`, and
`test_every_method_refuses_a_non_azure_scope` builds the scope with
`dataclasses.replace(scope, provider="openstack")` and asserts all four methods raise
`ScopeProviderMismatchError` with the injected executor's calls empty (use `ScriptedCommandExecutor`
from `tests/fakes/process.py` if lane `fakes-process-executor` has landed, else the module's
constructor-injected executor double; never a patch). Depends on child 3: before it, every spec is
stamped `openstack`, and this check would stop every Azure deploy.
