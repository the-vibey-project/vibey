<!-- audit: EPIC — a ten-item paper revision in one issue, labelled `bug`; the reviewer feedback it cites is not attached or tracked; the paper's substrate is changing under it (ADR-0044 RabbitMQ dispatch, 8.c two loops, 8.d GPT-OSS 20B, OpenCode repealed) -->

# Paper revision: formalize, broaden, and adversarially validate ledger-mediated orchestration

## Summary

A reviewer's feedback (targeting a top-tier venue, AAAI) found a strong, ledger-first
engineering contribution that is not yet validated enough for that bar. This epic tracks the
revision of `docs/paper.md` in ten workstreams:

1. a formal model with machine-checked artifacts, or narrowed claims
2. operational semantics for convergence, merge queues, stale judgments, leases and reapers
3. capacity-outranks-completion false negatives
4. starvation and fairness
5. ledger tamper evidence
6. broader throughput evidence
7. baselines
8. adversarial injection tests
9. handoff examples and ablations
10. reconciling the commodity-production thesis with all tracked evidence

The acceptance bar is that the paper, its formal model, experiments, threat model and
reproduction instructions agree with the implementation. Every number must come from
tracked sources. Three problems with the issue as filed:

- It is **labelled `bug`**, but it is a documentation/research epic.
- **The reviewer feedback it cites ("the attached reviewer feedback") is not in the issue
  or the repository**, so no one else can check which recommendations were adopted,
  narrowed or declined.
- The paper describes a substrate that is **changing now**: ADR-0044 moves dispatch to
  RabbitMQ, 8.c makes two loops, 8.d designates GPT-OSS 20B, and 8.b repeals OpenCode.
  Several sections must follow those changes regardless of the reviewer.

## Why

- **Doctrine 6** (`src/vibey_tools/gh/docs/doctrines.md:60-63`): a journal-grade paper,
  Markdown → LaTeX → PDF.
- **10.f** (`:419`): every claim tied to evidence with scope and cutoff. Unknown stays
  unknown.
- **8.g** (`:316-324`): measurements are continuous and published with the decisions they
  drive. The throughput evidence should come from those measurements.
- **9.c CDD** (`:351`): a verdict is not completion. The revision converges against tracked
  evidence.
- **Bill of Rights VI** (the Right to the Truth): limits stated before they are hit.
- **ADR-0032** (the docs ship as a paper and a book). **ADR-0044** "owes … the paper's
  *Queue semantics* and *Validation* sections".

## Current state

Evidence cutoff: integration tree `cce648ef`, read 2026-09-22.

- **`docs/paper.md`** (1,469 lines):
  - queue semantics `:163` (PostgreSQL `FOR UPDATE SKIP LOCKED`)
  - six-phase machine `:185`
  - CDD `:259`
  - engine family `:643`
  - exact-head `:809`
  - production rate and governance `:989`
  - validation `:1398-1418` (property, chaos and live levels; live runs on `claudeloop`
    and `agyloop` only; other engines rest on scripted-binary conformance)
  - related work `:1419`
  Figures are recomputed by `scripts/paper_evidence.py`. Artifacts: PDF, DOCX and HTML per
  channel (`.github/workflows/release-surfaces.yml:435-450`).
- **Recent paper commits:** `93fe3dcb` (one family paper), `cdc6e8d8` (#310, qwenloop storm
  evidence), `1eb31202` (#312), `7f7daee6` (#314, OpenCode).
- **No machine-checked artifacts:** no TLA+, Alloy or Lean files are tracked (`git ls-files`).
- **Relevant code for the adversarial workstream:**
  - `src/vibey/domain/prompt_shield.py` (untrusted-provenance prompt defence)
  - `domain/command_guard.py`
  - `domain/scope_guard.py`
  - `infrastructure/container/` (hardened container runtime)
- **Ledger integrity today:** each event carries a `digest` (`migrations/0013`), and
  handoff verifies a digest range (R6). There is no external anchoring.
- **Substrate changes the paper does not yet reflect:**
  - ADR-0044 (proposed) and QwenStorm #348–#381: RabbitMQ dispatch, loop services
  - 8.c: two loops, one instance per model
  - 8.d: GPT-OSS 20B on Ollama as the default
  - 8.b: OpenCode repealed, VS Code in its place
- The reviewer feedback: **not found** in the issue body, its comments, or any tracked
  file.

## Scope / Required behaviour

As in the original, with three additions:

- (a) the reviewer feedback tracked as a source file with an adoption table
- (b) the substrate update
- (c) the commodity-thesis reconciliation and the "entire development cycle" demand moved
  here from #155

## Acceptance criteria

The original four criteria stand, and:

- [ ] The reviewer feedback is tracked (for example `docs/paper/review-<date>.md`) with a
      table: recommendation → adopted / narrowed / declined → reason → paper section.
- [ ] The queue, engine-family and validation sections describe the substrate as merged at
      the paper's cutoff (ADR-0044, 8.c, 8.d, 8.b).
- [ ] Every formal claim is either machine-checked in CI or explicitly narrowed in the text.
- [ ] Every number is produced by a tracked script from tracked sources, or marked as an
      assumption.
- [ ] Release-surface checks pass for PDF and DOCX.
- [ ] The issue label is corrected from `bug` to a documentation/enhancement label.

## Dependencies and related issues

- **#155** (single paper source; its comprehensiveness half moves here).
- **#121** / ADR-0044 / #348–#381 (queue and loop semantics), **#134** (measured
  coordinates for the governance section), **#114** (ledger tamper evidence at scale).
- **#133** (the review lanes feature in exact-head evaluation).

## Proposed child issues

1. **Operator action — track the reviewer feedback.** Add the feedback text to the
   repository and create the adoption table. This blocks everything else.
2. **docs(paper): follow the substrate.** Update *Queue semantics* (`:163`), *The engine
   family* (`:643`) and *Validation* (`:1398`) for ADR-0044, 8.c, 8.d and the OpenCode
   repeal, as each lands. (One lane per section, docs only.)
3. **Design needed first — the formal model.** Choose a checker (TLA+/TLC, Alloy or Lean),
   specify leases, reaper, fenced acknowledgement and the no-double-commit property, and
   run it in CI. Otherwise narrow the claim.
4. **docs(paper): operational semantics for exact-head convergence, merge queues, stale
   judgments, leases and reapers.** Docs grounded in `vibey_gh/merge_train.py` and the job
   repository.
5. **feat(scripts): measure capacity-outranks-completion false negatives.** Extend
   `scripts/paper_evidence.py` to count completion claims overruled by capacity rejections
   in tracked ledgers, and their downstream cost.
6. **Design needed first — starvation, aging and fairness.** Analyse strict priority under
   the post-ADR-0044 claim order, with a simulation test.
7. **Design needed first — ledger tamper evidence.** An external anchor for the chain head
   (for example signed tags or a transparency log), and its threat model.
8. **Evidence — throughput across models, backends and hardware.** Use 8.g's continuous
   measurements: the model catalogue (#383), per-turn timing (#382), and Ollama and
   llama.cpp on the M5 class and an Arch Linux machine. Report uncertainty and timeout
   semantics.
9. **Design needed first — baselines.** Validate which named benchmarks fit before
   adopting any.
10. **test + docs: adversarial prompt, skill and tool injection.** A suite against
    `prompt_shield`, `command_guard` and `scope_guard`, and the sandbox and consistency
    boundary written into the threat model.
11. **docs(paper): handoff R1–R10 on messy briefs, with an ablation** against a plain
    summary.
12. **docs(paper): reconcile the commodity-production thesis** with the measured substrate
    and all tracked development-cycle evidence (from #155's comment).

## Open questions for the operator

1. **Where is the reviewer feedback?** Please attach or commit it. Without it, "which
   recommendations were adopted, narrowed or declined" cannot be recorded.
2. **Venue and deadline:** is AAAI the target, and is there a submission date that orders
   the workstreams?
3. **Formal methods budget:** machine-check the queue and handoff properties, or narrow
   the claims?
4. **Paper cutoff versus substrate churn:** should the paper describe the system as of a
   fixed release (for example the first release with RabbitMQ dispatch), or keep chasing
   develop?

## Original text

Issue #301, "Paper revision: formalize, broaden, and adversarially validate ledger-mediated orchestration", opened by @adammatthewsteinberger on 2026-09-19T17:30:57Z. Body, verbatim:

~~~~markdown
## Context

The attached reviewer feedback identifies a strong ledger-first engineering contribution, but says the paper is not yet sufficiently validated for a top-tier AAAI claim.

This issue tracks the paper work separately from product implementation. Reviewer comments are source material for prioritization, not binding repository instructions.

## Revision scope

- Formalize the six-phase state machine, no-loss handoff, safety/liveness, and concurrent handoff semantics; add machine-checked artifacts where practical or narrow claims accordingly.
- Add explicit operational semantics for exact-head convergence, merge queues, stale judgments, leases, and reapers.
- Quantify capacity-outranks-completion false negatives and downstream cost.
- Analyze strict-priority starvation, aging, fairness, and SLA behavior.
- Add cryptographic anchoring or tamper evidence for the core ledger.
- Broaden throughput evidence across models, serving backends, hardware, and realistic task pools; report uncertainty and timeout semantics.
- Add comparative baselines and validate which named benchmarks are appropriate before adopting them.
- Add adversarial prompt/skill/tool-injection tests and document the sandbox/consistency boundary.
- Add handoff R1-R10 examples on messy briefs and ablations against simpler summaries.
- Reconcile the commodity-production thesis with the actual measured substrate and all tracked development-cycle evidence.

## Acceptance criteria

- The paper's claims, formal model, experiments, threat model, limitations, and artifact/reproduction instructions agree with the implementation.
- Every reported number is generated from tracked sources or clearly marked as an assumption.
- The paper and book artifacts, including PDF and DOCX outputs, pass the release-surface checks.
- The final revision records which reviewer recommendations were adopted, narrowed, or declined and why.
~~~~
