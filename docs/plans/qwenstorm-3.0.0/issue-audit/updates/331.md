<!-- audit: TOO-BIG — nine production files (a new six-file `infrastructure/openstack` package, `.importlinter`, `az_cli.py`, `cli/main.py`) and four independent behaviours (Heat rendering, a CLI cloud adapter, the az adapter's preflight, worker flag and selection); also STALE: its CLI tests patch `vibey.cli.main.subprocess.run` and `PostgresJobReadyNotifier` (9.b, D7), the preflight is a raw `subprocess.run` in cli/main.py:1488-1490 rather than a declared seam, and it gives the `openstack` CLI (and the Heat plugin `openstack stack` needs) no install path on Arch Linux or macOS (8.h, installer wave) -->
<!-- title: feat(openstack): HeatTemplateRenderer renders a DeploymentSpec as a Heat template -->
Part of the **QwenStorm for 3.0.0**. A local sovereign lane (qwenloop until ADR-0046's rename lands) implements this issue; the result is reviewed and verified before it becomes a pull request.

This issue is **child lane 1 of 5** of "a real OpenStack CLI client, selected by [deploy].target and
`vibey worker --cloud`" (see *Proposed child lanes* at the end). It creates the
`src/vibey/infrastructure/openstack/` package with one class, the Heat template renderer. It runs no
subprocess and touches no CLI.

## Why
Sub-doctrine 8.b (`src/vibey_tools/gh/docs/doctrines.md:136-137` at `739536ea`) makes
**self-hosted OpenStack** the cloud default, and ADR-0042:58-64 promises "the self-hosted OpenStack
adapter (a real `openstack` CLI adapter plus the existing in-memory default)". Only Azure has a real
client (`src/vibey/infrastructure/azure/az_cli.py:47-165`), and it deploys by rendering the spec into
an ARM template (`src/vibey/infrastructure/azure/arm.py`). The OpenStack adapter (child lane 2) needs
the same thing for OpenStack's orchestration service, Heat: a template it can hand to
`openstack stack create|update --template FILE`. This lane builds that renderer, as a class with its
interface (ADR-0016; `arm.py`'s module function `render_template` is the older shape and is not
copied). The renderer follows `arm.py`'s rule (`arm.py:13-14`): an autonomous deploy never improvises
the shape of infrastructure, so anything it cannot render is refused.

## Required behaviour
1. `src/vibey/infrastructure/openstack/heat.py` declares `DEFAULT_IMAGE = "docker.io/library/nginx:stable"`,
   `class UnsupportedHeatTopology(ValueError)`, and `class HeatTemplateRenderer`.
2. `HeatTemplateRenderer(*, image: str = DEFAULT_IMAGE)` has a read-only property `image` and the
   method `render(spec: DeploymentSpec) -> dict[str, Any]`, which returns a HOT template.
3. `render` raises `UnsupportedHeatTopology` before building anything, checking in this order:
   - `spec.topology.iac_provider.lower() != "heat"` → reason `f"iac_provider {spec.topology.iac_provider!r}"`;
   - `spec.topology.service_type != "container_app"` → reason `f"service_type {spec.topology.service_type!r}"`;
   - `spec.topology.instances < 1` → reason `f"instances {spec.topology.instances}"`.
   The message is `f"{reason}: the Heat renderer runs iac_provider 'heat', service_type 'container_app' and at least one instance"`.
4. The template shape. With `app = "vibey-" + spec.spec_id[:20].lower().replace("_", "-")` (as at
   `arm.py:38`) and `scope = spec.target_scope`, the result has exactly these keys:
   - `"heat_template_version": "2018-08-31"`;
   - `"description": f"vibey deployment {spec.spec_id}"`;
   - `"parameters": {"image": {"type": "string", "default": <self.image>, "description": "Container image to run"}}`;
   - `"resources"`: when `spec.topology.ingress_enabled`, an `"ingress"` resource
     `{"type": "OS::Neutron::SecurityGroup", "properties": {"name": f"{app}-ingress", "rules": [{"direction": "ingress", "protocol": "tcp", "port_range_min": PORT, "port_range_max": PORT, "remote_ip_prefix": "0.0.0.0/0"}]}}`,
     where `PORT` is `443` when `spec.topology.tls_enabled` and `80` otherwise; then, for each `i` in
     `range(spec.topology.instances)`, a resource `f"app_{i}"`:
     `{"type": "OS::Zun::Container", "properties": {"name": f"{app}-{i}", "image": {"get_param": "image"}, "restart_policy": "always", "labels": {**scope.tags, "vibey.spec_id": spec.spec_id, "vibey.environment": scope.environment}, "security_groups": [{"get_resource": "ingress"}]}}`,
     where the `security_groups` key is present only when the `ingress` resource is;
   - `"outputs": {"endpoint": {"description": "Network addresses of the first container", "value": {"get_attr": ["app_0", "addresses"]}}}`.
5. Ingress rules, stated plainly:
   - `tls_enabled` opens only port 443, never a plaintext port. The image must terminate TLS itself:
     no Octavia load balancer is created.
   - `tls_enabled=False` opens port 80.
   - `ingress_enabled=False` gives no `ingress` resource and no `security_groups` key.
   - `OS::Zun::Container` has no port property, so the security group is how ingress is exposed.
     The properties used are those of `heat/engine/resources/openstack/zun/container.py`.
6. `render` is pure: no file, no subprocess, no network, and the same spec always gives an equal
   dict (child lane 2 writes it to a temporary file and hands it to the CLI).
7. `HeatTemplateRendererInterface` sits beside the class (ADR-0016, 9.b), in
   `src/vibey/infrastructure/openstack/interfaces/heat_interface.py`, and the new
   `vibey.infrastructure.openstack.interfaces` package is held by import-linter's
   `infrastructure-interfaces-declare-only` contract: interfaces declare and never consume.

The worked sample: a spec with `spec_id="spec-os-1"`, `tags={"owner": "vibey"}`,
`environment="dev"`, `instances=2`, `ingress_enabled=True`, `tls_enabled=True`, `iac_provider="heat"`,
`service_type="container_app"`, rendered with the default image, equals:
```json
{"heat_template_version": "2018-08-31",
 "description": "vibey deployment spec-os-1",
 "parameters": {"image": {"type": "string", "default": "docker.io/library/nginx:stable",
                          "description": "Container image to run"}},
 "resources": {
   "ingress": {"type": "OS::Neutron::SecurityGroup", "properties": {
      "name": "vibey-spec-os-1-ingress",
      "rules": [{"direction": "ingress", "protocol": "tcp", "port_range_min": 443,
                 "port_range_max": 443, "remote_ip_prefix": "0.0.0.0/0"}]}},
   "app_0": {"type": "OS::Zun::Container", "properties": {
      "name": "vibey-spec-os-1-0", "image": {"get_param": "image"}, "restart_policy": "always",
      "labels": {"owner": "vibey", "vibey.spec_id": "spec-os-1", "vibey.environment": "dev"},
      "security_groups": [{"get_resource": "ingress"}]}},
   "app_1": {"type": "OS::Zun::Container", "properties": {
      "name": "vibey-spec-os-1-1", "image": {"get_param": "image"}, "restart_policy": "always",
      "labels": {"owner": "vibey", "vibey.spec_id": "spec-os-1", "vibey.environment": "dev"},
      "security_groups": [{"get_resource": "ingress"}]}}},
 "outputs": {"endpoint": {"description": "Network addresses of the first container",
                          "value": {"get_attr": ["app_0", "addresses"]}}}}
```

## Where to change
Every new file starts with the provenance header that is line 1 of
`src/vibey/infrastructure/azure/az_cli.py`. Line numbers are from the storm integration branch at
`739536ea`.
- New `src/vibey/infrastructure/openstack/__init__.py`: a one-paragraph docstring (the self-hosted
  OpenStack cloud adapter, sub-doctrine 8.b) and exports of `DEFAULT_IMAGE`,
  `HeatTemplateRenderer` and `UnsupportedHeatTopology` in `__all__`. Child lane 2 adds its own names.
- New `src/vibey/infrastructure/openstack/heat.py`: Required behaviour 1-6. Module docstring in the
  style of `arm.py:1-15` (what it renders, why the image is a parameter, why unsupported shapes are
  refused). The renderer is a class; any helper is a method, not a module function.
- New `src/vibey/infrastructure/openstack/interfaces/heat_interface.py`:
  ```python
  @runtime_checkable
  class HeatTemplateRendererInterface(Protocol):
      @property
      def image(self) -> str:
          """The container image the template's `image` parameter defaults to."""
          ...

      def render(self, spec: DeploymentSpec) -> dict[str, Any]:
          """A HOT template for the spec, or UnsupportedHeatTopology -- never a guess."""
          ...
  ```
  Import only `typing` and `vibey.domain.deployment.DeploymentSpec`. Copy the docstring shape of
  `src/vibey/infrastructure/secrets/interfaces/openbao_interface.py:1-6`.
- New `src/vibey/infrastructure/openstack/interfaces/__init__.py`: docstring
  `"""Seams the OpenStack adapter declares. Interfaces declare; they never consume."""` and the
  export of `HeatTemplateRendererInterface`, following
  `src/vibey/infrastructure/secrets/interfaces/__init__.py`.
- `.importlinter`: after line 129 (`    vibey.infrastructure.tracker.interfaces`), add
  `    vibey.infrastructure.openstack.interfaces` to the `infrastructure-interfaces-declare-only`
  source list (contract at lines 108-133).
- New tests (below). No other file changes.

## Acceptance criteria
- [ ] `uv run pytest -q -p no:cacheprovider tests/infrastructure/openstack` passes.
- [ ] `uv run lint-imports` reports `infrastructure-interfaces-declare-only` KEPT with the new package
      listed, and `uv run pytest -q -p no:cacheprovider tests/meta/test_import_contracts_bind.py tests/application/test_interfaces_convention.py` passes.
- [ ] `git diff --stat` names only the four new source files, `.importlinter` and the new tests.
- [ ] `src/vibey/infrastructure/*` keeps 100% branch coverage.

## Tests to write first (TDD)
- New `tests/infrastructure/openstack/__init__.py`, holding only the header line, as in
  `tests/infrastructure/deploy/__init__.py`.
- New `tests/infrastructure/openstack/test_heat_renderer.py` (no database, no network, no marker,
  no patching). Build specs with a `_spec(**topology_changes)` helper modelled on `_spec()` in
  `tests/infrastructure/azure/test_az_cli.py:34-55`, with `spec_id="spec-os-1"`,
  `tags={"owner": "vibey"}`, `environment="dev"`, `provider="openstack"`, region `"RegionOne"`, and a
  `TopologyConfig("container_app", "heat", "m1.small", instances=2)`.
  - `test_render_matches_the_sample_template`: the whole dict equals the worked sample above.
  - `test_plain_ingress_opens_port_80`: `tls_enabled=False` gives one rule with 80/80.
  - `test_no_ingress_means_no_security_group`: `ingress_enabled=False` gives no `ingress` resource,
    and no `app_*` resource has a `security_groups` key.
  - `test_custom_image_is_the_parameter_default`: `HeatTemplateRenderer(image="registry.local/app:1")`
    puts that image in `parameters.image.default` and reports it through `.image`.
  - `test_render_refuses_what_it_cannot_run`: parametrized over `iac_provider="bicep"`,
    `service_type="kubernetes_fleet"` and `instances=0`; each raises `UnsupportedHeatTopology`
    matching `iac_provider 'bicep'`, `service_type 'kubernetes_fleet'` and `instances 0` respectively.
  - `test_render_is_deterministic`: two renders of one spec are equal and share no mutable
    sub-object (mutating one's `labels` leaves the other unchanged).
  - `test_renderer_satisfies_its_interface`: `isinstance(HeatTemplateRenderer(), HeatTemplateRendererInterface)`.

## Checks the lane must run (all must pass)
    uv run ruff check . && uv run ruff format --check .
    uv run mypy --strict src/vibey
    uv run lint-imports
    uv run bandit -q -r src/vibey
    uv run pytest -q -p no:cacheprovider tests/infrastructure/openstack tests/meta/test_import_contracts_bind.py tests/application/test_interfaces_convention.py
    uv run pytest -q -p no:cacheprovider --cov=vibey --cov-branch --cov-report=
    uv run coverage report --include='src/vibey/infrastructure/*' --fail-under=100
    uv run coverage report --include='src/vibey/domain/*' --fail-under=100
    uv run coverage report --include='src/vibey/application/*' --fail-under=100
    uv run coverage report --include='src/vibey/cli/*' --fail-under=100

One coverage run at a time. Until lane `fakes-harness-decouple` lands, `tests/conftest.py:146-156`
connects to PostgreSQL at session start, so the whole-suite run needs it; the new test files need
nothing and can run alone with `--noconftest -n 0`.

## Out of scope
- Child lanes 2-5: the `openstack` CLI adapter, the az adapter's preflight, `vibey worker --cloud`,
  and installing the CLI.
- TLS termination through Octavia/Barbican, mapping SKU to Zun cpu/memory, and choosing a network.
- AWS (8.b's default paid cloud) and GCP targets; `arm.py` and the Azure adapters.
- Do not edit CHANGELOG.md, docs/, ADRs, CLAUDE.md, AGENTS.md, GEMINI.md, README.md or the skill
  trees; the docs wave owns them. Do not push, open PRs, or change git remotes. Commit locally as
  `feat(openstack): HeatTemplateRenderer renders a DeploymentSpec as a Heat template`; the hooks add
  the `Made-With:` trailer.

**Depends on:** nothing unmerged. (#330's child lane 3 is what makes an OpenStack spec carry
`iac_provider = "heat"` by default; this renderer does not need it to be tested.)

## Hard repository rules (always)
- domain/ stays pure (no I/O, async, clock, network); enforced by tests/domain/test_domain_purity.py.
- Dependencies point inward: domain -> application -> infrastructure -> cli (import-linter).
- CreditsExhausted never has resets_at. A capacity rejection outranks a completion claim.
- Code lives in classes with an interface beside each (ADR-0016); module functions need a written reason.
  Exception classes carry no behaviour and follow the existing precedent of having no interface
  (`src/vibey/infrastructure/cache/redis.py:20`).
- Every job idempotent under replay; the ledger is append-only.
- Tests substitute at a declared seam, never by `monkeypatch.setattr`, `mock.patch` or `MagicMock`.

## Proposed child lanes

### 1. feat(openstack): HeatTemplateRenderer renders a DeploymentSpec as a Heat template (this issue)
The spec above: the new `infrastructure/openstack` package with the renderer and its interface,
`.importlinter`, and `tests/infrastructure/openstack/`. Independently mergeable. Depends on nothing.

### 2. feat(openstack): OpenStackCliAdapter drives Heat stacks through the `openstack` CLI
New `src/vibey/infrastructure/openstack/openstack_cli.py` and
`interfaces/openstack_cli_interface.py` (`OpenStackCliAdapterInterface(CloudClientPort, Protocol)`,
copying `secrets/interfaces/openbao_interface.py`), plus the two package exports. The adapter keeps
#331's original requirements B4-B11 unchanged in substance: constructor
`(*, executor: CommandExecutor | None = None, renderer: HeatTemplateRendererInterface | None = None, image: str = DEFAULT_IMAGE)`
(default executor `CleanGitEnvSubprocessExecutor()`, as `az_cli.py:48-55`); class attributes
`PROVIDER = "openstack"`, `PREFLIGHT_ARGV = ("openstack", "token", "issue", "-f", "value", "-c", "project_id")`
and `LOGIN_HINT`; the same refusal order in every method, all before any subprocess
(`ScopeProviderMismatchError` from #330's child 4, then digest-bound consent via
`MutationNotAuthorizedError` from `vibey.application.azure_port`, then `InvalidStackName(VibeyError)`
for a `resource_group` not matching `[A-Za-z][A-Za-z0-9_.-]{0,254}`, then `UnsupportedHeatTopology`
for `execute_plan`); every argv `("openstack", "--os-cloud", subscription_id, "--os-project-id", tenant_id, "--os-region-name", region, *tail)`;
the command table (token issue + `stack resource list`; `stack show` probe then `stack create` or
`stack update --template FILE --wait`, then `stack show`; `stack show -c id -c stack_status`;
`stack delete --yes --wait`, idempotent on "stack not found"); the temporary template file unlinked
in a `finally`; `stack_state()` mapping; `OpenStackCliError(argv, stderr)` with stderr only. New:
`async def preflight(self) -> bool` runs `PREFLIGHT_ARGV` through the injected executor and returns
whether it exited 0 (an `OSError`, meaning the CLI is not installed, is `False`), so no caller needs
its own `subprocess.run`. Tests in `tests/infrastructure/openstack/test_openstack_cli.py`, all
through the constructor's `executor=` seam: `ScriptedCommandExecutor` from `tests/fakes/process.py`
if lane `fakes-process-executor` has landed on the integration branch when this lane starts, else a
scripted executor class in the test module returning `CommandResult`
(`src/vibey/infrastructure/engines/claudeloop_process.py:28`); the original test list (discovery,
create/update/failed create, malformed show output, consent refusals, provider refusal, invalid stack
name, unsupported topology, the status table, idempotent delete, error text, port conformance) plus
`test_preflight_reports_a_missing_cli_as_not_ready`. Depends on child 1 and #330 child 4.

### 3. feat(azure): the az adapter declares its provider and preflight
`src/vibey/infrastructure/azure/az_cli.py` only: class attributes `PROVIDER = "azure"`,
`PREFLIGHT_ARGV = ("az", "account", "show", "-o", "none")` and
``LOGIN_HINT = "a logged-in Azure CLI: run `az login` first"``; `_require_provider` (from #330's
child 4) compares against `self.PROVIDER`; the same `async def preflight(self) -> bool` as child 2,
through the injected executor; the module docstring (`:1-16`) names `vibey worker --cloud cli` with
`[deploy].target = "azure"`. Tests in `tests/infrastructure/azure/test_az_cli.py`
(`test_az_preflight_is_declared_on_the_adapter`, `test_az_preflight_is_ready_only_when_the_account_shows`),
through the constructor's `executor=` seam. Depends on #330 child 4. Independent of child 2.

### 4. feat(cli)!: `vibey worker --cloud {memory,cli}` runs the declared target's CLI adapter; `--azure` becomes a hidden alias
`src/vibey/cli/main.py` plus a small selection seam: a new class `CloudClientSelector`
(`src/vibey/infrastructure/cloud/selector.py`, interface beside it, package added to the
import-linter contract) mapping a `[deploy].target` to its adapter class (`azure` →
`AzCliClientAdapter`, `openstack` → `OpenStackCliAdapter`), building it with an injectable
`CommandExecutor`, and returning it only when `await adapter.preflight()` is true. The worker reads
it from `CliComposition` (lane `fakes-job-wakeup`), so tests pass
`runner.invoke(app, [...], obj=CliComposition(..., cloud_clients=CloudClientSelector(executor=ScriptedCommandExecutor(...))))`
and nothing patches `vibey.cli.main.subprocess.run` or `PostgresJobReadyNotifier`. Behaviour as
#331's original D13-D17: `--cloud` (`memory` default, `cli`) replaces `--azure` (`:1451-1459`), which
stays hidden for one major version (`az` means `--cloud cli`, only when the target is `azure`;
passing both is exit 2); flag checks run before `run_worker` (`:1481-1494`, exit 2); after
`provider = _resolve_provider(provider_opt)` (`:1573`) the project's `[deploy]` is resolved from its
stored config, else its repository's `vibey.toml` for projects created before 3.0.0 (a `ConfigError`
exits 3 through `guard()`, `src/vibey/cli/errors.py:90`); `--cloud cli` with no ready CLI exits 1
naming the adapter's `LOGIN_HINT`; `build_full_worker(..., azure_client=cloud_client, deploy=deploy)`
(`:1699-1711`); the "worker started:" line (`:1716-1719`) gains ` deploy_target=... cloud=...`.
Convert `test_worker_azure_az_requires_a_logged_in_cli` and
`test_worker_azure_az_builds_the_real_adapter_when_logged_in`
(`tests/cli/test_operational_commands.py:1991-2028`) off patching, and add #331's original nine
worker tests the same way; tests that seed a project stay `integration` (PostgreSQL through the real
`build_app()`), and the selector's own unit tests are the default-tier proof. Depends on children 2
and 3, #330 children 1 and 3, and `fakes-job-wakeup`.

### 5. feat(install): the OpenStack CLI and its Heat plugin are an opt-in dependency on Arch Linux and macOS
Add an entry to the installer wave's catalogue (`src/vibey/domain/local_stack.py`, lane
`installer-catalogue`) in an opt-in `cloud` group (default False), installed by `HostPackageRunner`
(never a piped remote script): on Arch Linux through pacman or the AUR helper, on macOS through
Homebrew. `openstack stack …` needs the Heat client plugin as well as the base client, so the entry
must provide both. The lane verifies the package names on the day (`pacman -Si python-openstackclient
python-heatclient`, the AUR helper's `-Si` for any not in the official repositories, and
`brew info openstackclient`, confirming the formula ships the `stack` commands), records them with
that evidence and date in the entry's `note` (10.f), and uses `openstack` as the binary probe. If the
installer exposes groups on the command line (lane `installer-cli`), `--with cloud` selects it. The
Azure CLI is a paid, declared-only cloud's tool and is not added here. Depends on
`installer-catalogue` and `installer-toolchain`; independent of children 1-4.
